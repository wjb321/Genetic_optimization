clc;
clear;
close all;

%% define the problem
%CostFunc = @MinOne;
Problem.CostFunc = @(x) MinOne(x);
Problem.nVar = 10; %parameter variable


%% GA parameters 
params.MaxIt = 100;
params.nPop = 20; %the initial generation value
params.pC = 1;
nC = round(pC*nPop);
%% Run GA to solve problem
out = RunGA(Problem, params);


%% results